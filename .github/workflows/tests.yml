name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.8'
      SERVERPOD_PASSWORD_database: 'a4xvTWLQpv0qUR13Q6XTDyiEP78HtF1k'
      SERVERPOD_PASSWORD_redis: 'WrK7TXMettDt6zJMn4OxKoSUg0jM3MxH'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: butler_backend_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: butler_backend_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: butler_backend_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: butler_backend_server
        run: serverpod generate

      - name: Run tests
        working-directory: butler_backend_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: butler_backend_server
        run: docker compose down -v
